Make the expense form looks like it's just one and each line you add just scratch him with another line.

Make light mode more white with black contrast.

Remove "add new expenses" and put a button with "+" beside each line, when you pressed you add a line below and when you pressed the "+" turn into a "-" or a trash icon, when you press you remove the line you pressed.
Remember just one line can have the plus icon.

For the page remove all code and create a home path and for default page load the home. Create a fixed bar(when you scroll it's still appearing).

For budget cards you can create a component and import that component, remember now the add button is part of the card

For result planner you can create a component too and import in a home page.


Follow these steps:
1. Use frontend-design skill to define layout
2. Use shadcn components for UI
3. Follow next-best-practices for Next.js structure